import { FC } from 'react';

import Layout from '@/components/layout/Layout';

const Conversations: FC = () => {
	return <Layout title={'conversations'}>Conversations</Layout>;
};

export default Conversations;
